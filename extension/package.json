{
  "name": "ai-tells",
  "version": "0.1.0",
  "description": "AI Tells — a browser extension that helps you identify signs of probable AI usage by highlighting LLM writing tells on any web page. Adapted from Simon Willison's llm-cliche-highlighter and Wikipedia's Signs of AI writing guide.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:chrome": "node build.mjs chrome",
    "build:firefox": "node build.mjs firefox",
    "build:safari": "node build.mjs safari",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.11",
    "@vitest/coverage-v8": "^4.1.11"
  }
}
